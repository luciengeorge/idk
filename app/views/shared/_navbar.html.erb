<div class="navbar-wagon">
  <!-- Right Navigation -->
  <div class="navbar-wagon-right hidden-xs hidden-sm">

    <% if user_signed_in? %>

      <!-- Links when logged in -->
      <% conversations = Conversation.where(recipient_id: current_user.id).or(Conversation.where(sender_id: current_user.id)) %>
      <% unread_messages = [] %>
      <% conversations.each do |conversation| %>
        <% conversation.messages.each do |message| %>
          <% unread_messages << message %>
        <% end %>
      <% end %>
      <% unread_messages = unread_messages.reject { |message| message.read || message.user_id == current_user.id }.count %>
      <% if unread_messages.zero? %>
        <%= link_to "INBOX", conversations_path, class: "navbar-wagon-item navbar-wagon-link" %>
      <% else %>
        <%= link_to "INBOX (#{unread_messages})", conversations_path, class: "navbar-wagon-item navbar-wagon-link unread" %>
      <% end %>
      <%= link_to "WISHLIST", wishes_path, class: "navbar-wagon-item navbar-wagon-link" %>
      <%= link_to "CATEGORIES", categories_path, class: "navbar-wagon-item navbar-wagon-link" %>

      <!-- Avatar with dropdown menu -->
<!--       <div class="navbar-wagon-item">
        <div class="dropdown">
          <i class="fas fa-bars dropdown-toggle" id="navbar-wagon-menu" data-toggle="dropdown"></i>
          <ul class="dropdown-menu dropdown-menu-right navbar-wagon-dropdown-menu">
            <li>
              <%= link_to "#" do %>
                <i class="fa fa-user"></i> <%= t(".profile", default: "Profile") %>
              <% end %>
            </li>
            <li>
              <%= link_to "#" do %>
                <i class="fa fa-cog"></i>  <%= t(".settings", default: "Settings") %>
              <% end %>
            </li>
            <li>
              <%= link_to destroy_user_session_path, method: :delete do %>
                <i class="fa fa-sign-out"></i>  <%= t(".sign_out", default: "Log out") %>
              <% end %>
            </li>
          </ul>
        </div>
      </div> -->
    <% else %>
      <!-- Login link (when logged out) -->
      <%= link_to t(".sign_in", default: "LOGIN"), new_user_session_path, class: "navbar-wagon-item navbar-wagon-link" %>
      <%= link_to "CATEGORIES", categories_path, class: "navbar-wagon-item navbar-wagon-link" %>
    <% end %>
  </div>

  <!-- Dropdown list appearing on mobile only -->
  <div class="navbar-wagon-item hidden-md hidden-lg">
    <% if user_signed_in? %>
      <div class="dropdown">
        <i class="fa fa-bars dropdown-toggle" data-toggle="dropdown"></i>
        <ul class="dropdown-menu navbar-wagon-dropdown-menu">
          <% conversations = Conversation.where(recipient_id: current_user.id).or(Conversation.where(sender_id: current_user.id)) %>
          <% unread_messages = [] %>
          <% conversations.each do |conversation| %>
            <% conversation.messages.each do |message| %>
              <% unread_messages << message %>
            <% end %>
          <% end %>
          <% unread_messages = unread_messages.reject { |message| message.read || message.user_id == current_user.id }.count %>
          <% if unread_messages.zero? %>
            <li><%= link_to "INBOX", conversations_path, class: "navbar-wagon-item navbar-wagon-link" %></li>
          <% else %>
            <li><%= link_to "INBOX (#{unread_messages})", conversations_path, class: "navbar-wagon-item navbar-wagon-link unread" %></li>
          <% end %>
          <li><%= link_to "WISHLIST", wishes_path, class: "navbar-wagon-item navbar-wagon-link" %></li>
          <li><%= link_to "CATEGORIES", categories_path, class: "navbar-wagon-item navbar-wagon-link" %></li>
        </ul>
      </div>
    <% else %>
      <%= link_to t(".sign_in", default: "Login"), new_user_session_path, class: "navbar-wagon-item navbar-wagon-link" %>
      <%= link_to "CATEGORIES", categories_path, class: "navbar-wagon-item navbar-wagon-link" %>
    <% end %>
  </div>

  <!-- Logo -->
  <%= link_to root_path do %>
    <p class="logo-text">idk<strong class="logo-period">.</strong></p>
  <% end %>

  <% if user_signed_in? %>
    <!-- right div dissapearing on mobile -->
    <div class="hidden-sm hidden-xs">
      <div class="navbar-wagon-item">
        <div class="dropdown">
          <i class="fas fa-bars dropdown-toggle" id="navbar-wagon-menu" data-toggle="dropdown"></i>
          <ul class="dropdown-menu dropdown-menu-right navbar-wagon-dropdown-menu">
            <li>
              <%= link_to profile_path do %>
                <i class="fa fa-user"></i> <%= t(".profile", default: "Profile") %>
              <% end %>
            </li>
            <li>
              <%= link_to destroy_user_session_path, method: :delete do %>
                <i class="fa fa-sign-out"></i>  <%= t(".sign_out", default: "Log out") %>
              <% end %>
            </li>
          </ul>
        </div>
      </div>
    </div>
  <% else %>
    <div></div>
  <% end %>
</div>

