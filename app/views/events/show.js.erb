users = document.querySelector(".user_choices");
users.innerHTML = "";
<% @followers_array.each do |follower| %>
  followerAvatar = "<%= j render 'user_info', user: follower %>";
  users.insertAdjacentHTML('beforeend', followerAvatar);
<% end %>

// $(document).ready(function(){
//   $(".user-info").click(function(){
//     $(this).toggleClass("opaque");
//   });
// });

userChoices = document.querySelectorAll('.user_choice');
userChoices.forEach((choice) => {
  choice.addEventListener('click', () => {
    id = choice.dataset.userId;
    document.getElementById(`user_avatar_${id}`).classList.toggle('active');
    document.getElementById(`name_${id}`).classList.toggle('active');
  });
});
