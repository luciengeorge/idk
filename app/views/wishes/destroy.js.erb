console.log('destroy');

// wishButton = document.querySelector("[data-wish-id='<%= @activity.id %>']").remove()
// wishButton.addEventListener(('click'), () => {
//   wishButton.innerText = 'Add wish to list'
// });
